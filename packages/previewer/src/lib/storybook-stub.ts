/**
 * Runtime stub for @storybook/react.
 * Allows story files that `import { Meta, StoryObj } from "@storybook/react"`
 * to resolve without the actual Storybook dependency.
 * These imports are only used as types and are elided at runtime anyway.
 */
export {}
